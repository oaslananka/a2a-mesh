# `a2a-mesh-registry`

Registry features include:

- agent registration and heartbeat updates
- skill- and tag-based search
- in-memory and Redis storage
- health checks and SSE updates for registry consumers
