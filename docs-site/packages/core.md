# `a2a-mesh`

Core provides the server runtime, task lifecycle, health endpoint, auth middleware, retry-aware client, telemetry helpers, and storage abstractions.

Key exports:

- `A2AServer`
- `TaskManager`
- `JwtAuthMiddleware`
- `InMemoryTaskStorage`
- `SqliteTaskStorage`
