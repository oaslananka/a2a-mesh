# `a2a-mesh-testing`

Advanced companion package for in-process A2A testing.

It is kept in the monorepo for teams that want reusable fixtures and test harnesses, but it is **not part of the first public npm release wave**.

```ts
import { A2ATestServer, MockA2AClient } from 'a2a-mesh-testing';
```
