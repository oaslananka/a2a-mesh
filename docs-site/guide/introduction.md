# Introduction

`a2a-mesh` helps TypeScript teams build interoperable agents without hand-writing the A2A transport, task lifecycle, streaming, or authentication glue every time.

Use it when you want:

- protocol-correct agent-card and JSON-RPC behavior
- framework adapters instead of provider-specific boilerplate
- testable task orchestration with shared `contextId`
- registry-based discovery across multiple agents
