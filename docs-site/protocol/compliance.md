# Compliance

`a2a-mesh` targets A2A Protocol v1.0 and includes:

- canonical `/.well-known/agent-card.json`
- JSON-RPC message, task, and push-notification flows
- extension negotiation
- authenticated extended-card support
