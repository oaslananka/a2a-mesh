# Changelog

The release log is maintained in the repository root `CHANGELOG.md`.

Public releases are cut with Changesets, and docs deployments are handled manually from built
artifacts rather than GitHub Actions.
